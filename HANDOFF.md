# mirror-vn — HANDOFF
Last session: 2026-07-11 | Status: active | Branch: main (docs 논의만 — 코드/PRD 파일 변경 없음)

## Where we left off
**PRD §4.2 Phase 2 각본 결정 세션.** 지난 세션에서 발견한 "귀신 ✊ 고정 + 무조건 패배" 논리 결함(유저 보 = RPS상 승리)을 놓고 여러 안을 검토, 원작 재확인까지 거쳐 아래로 수렴.

**진행 경과:**
- 원작 트랜스크립트(`docs/scenario.md`) 재확인: Phase 1 규칙("보 내면 비김, 바위/가위 내면 귀신 근접")은 §4.1과 일치. Phase 2는 "가위만 이김. 귀신은 가위를 낼 수 없다" + "응하지 않으면 판정승을 넘긴다"(귀신이 판정권 보유) — 원작 자체에 매 라운드 귀신 손 결정 메커니즘은 명시 안 됨
- "§4.1을 Phase 2에 그대로 적용" 검토 → 100% 결정론이면 유저는 가위를 내도 무조건 패배(승리 케이스 0%), 사용자 의도("가위만 유일한 승리 경로, 낮은 확률")와 불일치
- 사용자가 원래 의도했던 방향(B) 확인: 귀신 손을 확률적으로 결정 — 단, 옛 구현(`src/hooks/useGameState.ts:16`, `Math.random() < 0.2` 20% 랜덤 승리, 제스처 무관)과는 다름. 그건 결정 로그 #1에서 "가위만 이김" 힌트와의 자기모순으로 이미 폐기된 방식
- **최종 수렴 테이블** (보/바위 확률 커스텀 불필요 = 기본 50/50):

  | 유저 손 | 귀신 손 | 판정 |
  |---|---|---|
  | 보 | 보 (결정론, 미러링) | 비김 |
  | 바위 | 보 (결정론) | 패배 |
  | 가위 | 보 (50%) | 승리 |
  | 가위 | 바위 (50%) | 패배 |

  랜덤은 "유저가 가위를 냈을 때 귀신이 보/바위 중 뭘 낼지"에만 걸림. 보/바위는 항상 결정론적으로 비김/패배 — 가위가 유일한 승리 경로라는 원작 속성 보존. R3(펜 트릭)는 이 표와 무관하게 여전히 결정론적 확정승(의식 파탄) 유지.

## What's next
**미결 하위 질문에 사용자 답변 받고 → PRD §4.2 + 핵심 원칙 2 업데이트.**
1. R1 또는 R2에서 이 50% 럭키 승리가 실제로 나오면 어떻게 처리하는지 (A: 바로 WIN_CUTSCENE 스킵 — Q1/Q2/인사이트/펜트릭 다 건너뜀 / B: 판정권 조항으로 무시하고 패배 처리 — 근데 이러면 랜덤 넣는 의미가 없어짐, Claude는 A 추천)
2. 답변 받으면: PRD §4.2 표 교체 + 핵심 원칙 2("난수 0%")를 "Phase 1·R3는 난수 0%, Phase 2 R1/R2는 예외" 형태로 조건부 수정 + §9 Open Question #1 제거(해결됨) + FSM 분기 노트 추가(A 선택 시 YEO-599에서 럭키 승리 스킵 경로 반영)
3. PRD 확정 후 YEO-599(데이터 주도 씬 시스템) 착수

## Open decisions
- **R1/R2 럭키 승리 처리: A(WIN_CUTSCENE 스킵) vs B(판정권으로 무시)** — Claude는 A 추천, 사용자 답변 대기
- PRD 기존 Open Questions 중 남은 것: Bad Ending 99조 문안 / ROUND3_SETUP 독백 문안 (전부 문안 작업, 급하지 않음). Q3 삽입 지점은 이번 세션과 무관, 그대로 미정
- 모델 전략: 기본 Sonnet, Fable 필요 항목만 전환 — [4] Phase 2 위화감 처리·R3 독백, [3] 톤 필터 감각 튜닝, [6] 99조 문안, [7] 페르소나 프롬프트+인젝션 방어

## Context for Claude
- `docs/PRD.md`가 모든 판정 규칙의 단일 출처 — 코드는 이 문서의 테이블을 데이터로 옮긴 것이어야 함. 문서에 없는 규칙 구현 금지
- **핵심 원칙 2("난수 0%")는 이번 결정으로 예외가 생김** — Phase 2 R1/R2 가위 분기만 `Math.random` 허용. PRD 업데이트 시 원칙 문구 자체를 조건부로 명시할 것 (전면 폐기 아님)
- 옛 구현의 `useGameState.ts:16` 20% 랜덤 승리 코드는 참고용으로만 남아있음 — 새 로직은 그것과 다른 메커니즘(제스처 의존적, 가위 분기에만 랜덤)이니 그대로 재사용하지 말 것
- 핵심 원칙: 1인칭 시점 / 규칙 텍스트=판정 일치 / 억울한 죽음 금지 / 명령형 튜토리얼 금지 (안내 3겹: 캘리브레이션·[시스템] 라인·인식 인디케이터) / 원작 지식 전제 (인트로 힌트 심기 없음)
- 서브이슈 구현 순서: YEO-598(기획, In Review) → 599(씬 FSM) → 600(입력+캘리브레이션) → 601(레이어) → 602(RPS) → 603(탈출) → 604(엔딩) → 605(LLM) → 606(사운드/폴리시). 각 = 1브랜치 + 1PR
- 기존 코드 재사용 가치: `cbRef` 패턴 (useHandTracking), Canvas 서리 닦기. 현행 HoldButton은 포즈 판정 + 1프레임 즉사 — 리빌드에서 위치 판정으로 교체
- `src/components/scene/TornNotice.tsx` untracked로 계속 남아 있음 (구 구현 잔재, 5/31 세션) — 리빌드 방향 확정됐으니 정리 대상이나 아직 삭제 승인 안 받음
- Production: https://mirror-vn.vercel.app | 레포: yhyh6565/cctv-horror-game-fin
