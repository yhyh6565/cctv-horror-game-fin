# mirror-vn — HANDOFF
Last session: 2026-07-13 | Status: **paused** | Branch: main (docs 논의만 — 코드/PRD 파일 변경 없음)

## Where we left off
**프로젝트 일시중지.** 2026-07-13 세션에서 YEO-597 진행 상황 리뷰 + Phase 2 각본 규칙 추가 논의 후, 사용자 결정으로 프로젝트를 paused로 전환. Linear: 프로젝트 상태 Paused, 전 티켓 Backlog 이동 (YEO-598 In Review 포함).

**이번 세션 논의 내용:**
- 사용자 질문: "유저 바위면 귀신도 바위 낼 수 있지 않나?" → Claude 답변: PRD §4.1 귀신 손 결정 규칙("유저를 이기는 손, 가위 불가 시 미러링")상 바위→바위 비김은 규칙 위반. 현행 유지(유저 바위 → 귀신 보 → 패배) 추천 — 규칙 단일성, 바위 패널티, "가위 막힐 때만 미러링" 단서 패턴 보존. **사용자 확답은 안 받음.**
- 사용자 질문: "귀신이 지거나 비기기만 하기로 했었나?" → 아님을 확인. 판정 컬럼은 유저 기준, 귀신은 기본적으로 이기는 쪽.
- 새 미결 발견: Phase 2에서 유저 보로 **비겼을 때** 라운드 처리(재대결/라운드 소진/판정권 행사)가 수렴 테이블에 미명시.

## What's next (재개 시)
**미결 질문 3개에 답 받고 → PRD §4.2 + 핵심 원칙 2 업데이트 → YEO-599 착수.**
1. R1/R2 럭키 승리 처리: A(WIN_CUTSCENE 스킵) vs B(판정권으로 무시) — Claude는 A 추천
2. 유저 바위 → 귀신 손: 현행(보, 패배) 유지 vs 바위 비김 허용 — Claude는 현행 유지 추천
3. Phase 2 비김(유저 보) 시 라운드 처리 방식 — 미논의

## Open decisions
- 위 3개 미결 질문 (전부 PRD §4.2 박제의 전제)
- PRD 기존 Open Questions: Bad Ending 99조 문안 / ROUND3_SETUP 독백 문안 (문안 작업, 급하지 않음) / Q3 삽입 지점 미정
- 모델 전략: 기본 Sonnet, Fable 필요 항목만 전환 — [4] Phase 2 위화감 처리·R3 독백, [3] 톤 필터 감각 튜닝, [6] 99조 문안, [7] 페르소나 프롬프트+인젝션 방어

## Context for Claude
- **재개 시 Linear 원복 필요:** 프로젝트 Paused → 원래 상태, 티켓들 Backlog → Todo (YEO-598은 In Review였음)
- 2026-07-11 수렴 테이블 (PRD 미반영 — 세션 로그 2026-07-11 참조): 보→보 비김(결정론) / 바위→보 패배(결정론) / 가위→보 50% 승 / 가위→바위 50% 패. 랜덤은 가위 분기에만. R3 펜 트릭은 결정론적 확정승 유지
- `docs/PRD.md`가 모든 판정 규칙의 단일 출처 — 코드는 이 문서의 테이블을 데이터로 옮긴 것이어야 함. 문서에 없는 규칙 구현 금지
- 핵심 원칙 2("난수 0%")는 수렴안 채택 시 조건부 수정 필요 (Phase 2 R1/R2 가위 분기만 예외, 전면 폐기 아님)
- 옛 구현 `useGameState.ts:16` 20% 랜덤 승리 코드는 폐기된 방식 — 재사용 금지
- 핵심 원칙: 1인칭 시점 / 규칙 텍스트=판정 일치 / 억울한 죽음 금지 / 명령형 튜토리얼 금지 (안내 3겹) / 원작 지식 전제
- 서브이슈 구현 순서: YEO-598(기획) → 599(씬 FSM) → 600(입력+캘리브레이션) → 601(레이어) → 602(RPS) → 603(탈출) → 604(엔딩) → 605(LLM) → 606(사운드/폴리시). 각 = 1브랜치 + 1PR
- YEO-602 AC "판정 로직에 Math.random 없음"은 수렴안과 충돌 — PRD 업데이트 시 티켓 AC도 수정 필요
- 기존 코드 재사용 가치: `cbRef` 패턴 (useHandTracking), Canvas 서리 닦기. 현행 HoldButton은 포즈 판정 + 1프레임 즉사 — 리빌드에서 위치 판정으로 교체
- `src/components/scene/TornNotice.tsx` untracked 잔재 (구 구현, 5/31) — 정리 대상이나 삭제 승인 안 받음
- Production: https://mirror-vn.vercel.app | 레포: yhyh6565/cctv-horror-game-fin
